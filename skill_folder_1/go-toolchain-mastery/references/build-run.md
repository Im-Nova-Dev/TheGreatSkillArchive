# Build and Run

- `go build ./...`
- `go run ./cmd/myapp`
- `go install` for local tools
- `go build -o bin/myapp ./cmd/myapp`

## Environment
- `GOPATH` for workspace and installed tools
- `GOMODCACHE` for module cache
- `GOFLAGS`, `GOENV` for local overrides
