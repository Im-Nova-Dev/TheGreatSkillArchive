# Session Zero Script

The very first class: install, configure, and calm fears.

## Welcome
> “Today we make your terminal a safe place.”

## Install Check
- `git --version`
- `gh --version`
- `python3 --version`

## Config
```
git config --global user.name "Your Name"
git config --global user.email "you@example.com"
```

## First Victory
Create a repo, add a file, commit, and push in 5 minutes.

## Mental Model
- Git = memory
- Staging area = planning
- Commit = snapshot
- Branch = alternate timeline
