# Teaching Moments Bank

Curated micro-lessons triggered by real learner states.

## When `git status` is ignored
Mini-lesson: `git status` is a compass, not a report card.

## When they force-push without care
Mini-lesson: `--force-with-lease` vs `--force`.

## When history looks scary
Mini-lesson: Git remembers almost everything. Show reflog.

## When conflicts happen
Mini-lesson: Conflict markers = conversation. Read both sides before choosing.

## When merges create ugly history
Mini-lesson: merge vs rebase trade-offs.
